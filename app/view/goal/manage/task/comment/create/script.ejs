<script>
  async function commentCreate(comment) {
    let response = await API.response(GoalTaskComment.create, comment);
    if (!response) { return false; }

    lib.message(response);
  };
</script>