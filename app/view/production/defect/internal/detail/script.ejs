<script>
  async function receiptProductDetail(products) {
    // lib.confirm("Confirmar retirada para conserto?", async r => {
    //   if (!r) { return; }

    //   let response = await API.response(Production.receipt.product.update,);
    // });

    lib.auth("Aqui vai um texto?", async pass => {
      console.log(pass);
    });
  };
</script>